#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
//typedef vector<int> vi;

set<int> letters;

int maybe(map<char, int> m)
{

}

int solve(map<char, int> m)
{
  
}

int main()
{
  string a, b, c; cin >> a >> b>> c;
  for (char k : a) letters.insert(k);
  for (char k : b) letters.insert(k);
  for (char k : c) letters.insert(k);

  return 0;
}
