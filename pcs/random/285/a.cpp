#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define CONTAINS(set, x) (set.find(x) != set.end())

const double EPS = 1e-5;

template <class T>
T tsearch(function<T(T)> f, T l, T r, function<bool(T)> done) {
    T mid = (l+r)/2;
    while (1) {

    }
    return mid;
}

void Solve()
{
    cout << fixed << setprecision(6) << EPS;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    Solve();
    return 0;
}

