#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    return 0;
}

// iff all wind push away: impossible
// (there must be at least one helpful wind in each
// direction of necessary travel)
// 
